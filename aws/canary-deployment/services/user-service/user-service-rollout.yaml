apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: user-service-rollout
  namespace: app
  labels:
    app: user
    service: user-service
spec:
  replicas: 1

  strategy:
    canary:
      steps:
      - setWeight: 10
      - pause: {duration: 30s}
      - analysis:
          templates:
          - templateName: user-service-success-rate
          args:
          - name: service-name
            value: user-service
      - setWeight: 25
      - pause: {duration: 60s}
      - analysis:
          templates:
          - templateName: user-service-success-rate
          args:
          - name: service-name
            value: user-service
      - setWeight: 50
      - pause: {duration: 120s}
      - analysis:
          templates:
          - templateName: user-service-success-rate
          args:
          - name: service-name
            value: user-service
      - setWeight: 100

      canaryService: user-service-canary
      stableService: user-service-stable
      
      trafficRouting:
        alb:
          ingress: app-ingress
          servicePort: 80

  revisionHistoryLimit: 10

  selector:
    matchLabels:
      app: user
      service: user-service

  template:
    metadata:
      labels:
        app: user
        service: user-service
    spec:
      nodeSelector:
        eks.amazonaws.com/nodegroup: app-ng
      tolerations:
        - key: "app"
          operator: "Equal"
          value: "true"
          effect: "NoSchedule"
      
      terminationGracePeriodSeconds: 30

      containers:
      - name: user-service
        image: ${ECR_REGISTRY}/${ECR_PREFIX}/user-service:${IMAGE_TAG}
        ports:
        - containerPort: 8014
          name: http
          protocol: TCP

        # ConfigMap과 Secret에서 환경변수 로드 (optional로 변경)
        envFrom:
        - configMapRef:
            name: app-config
            optional: true
        - secretRef:
            name: app-secrets
            optional: true

        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "prod"

        # 헬스체크 (HTTP로 변경하여 정확성 향상)
        startupProbe:
          httpGet:
            path: /actuator/health
            port: 8014
          initialDelaySeconds: 60
          periodSeconds: 10
          failureThreshold: 30

        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8014
          initialDelaySeconds: 30
          periodSeconds: 5
          failureThreshold: 3

        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: 8014
          initialDelaySeconds: 90
          periodSeconds: 30
          failureThreshold: 3

        resources:
          requests:
            cpu: "200m"
            memory: "512Mi"
          limits:
            cpu: "1000m"
            memory: "1Gi"

        securityContext:
          allowPrivilegeEscalation: false
          runAsNonRoot: true
          runAsUser: 1000
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: true

        volumeMounts:
        - name: tmp-volume
          mountPath: /tmp
        - name: logs-volume
          mountPath: /app/logs

      volumes:
      - name: tmp-volume
        emptyDir: {}
      - name: logs-volume
        emptyDir: {}

      securityContext:
        fsGroup: 1000
        runAsGroup: 1000
        runAsUser: 1000